@model IEnumerable<FoodYeah.Model.Customer_Category>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Customer_CategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer_CategoryDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Customer_CategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer_CategoryDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Customer_CategoryId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Customer_CategoryId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Customer_CategoryId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
